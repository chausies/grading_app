<% provide(:title, @course.name + " | " + @new_enrollment.participant.name) %>
<h1><%= @course.name + ": " + @new_enrollment.participant.name %></h1>
<div class="row">
  <div class="span10 offset1">
		<%= render 'enrollment_view', new_enrollment: @new_enrollment %>
  </div>
</div>
