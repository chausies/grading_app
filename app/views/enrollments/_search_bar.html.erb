<%= form_tag course_enrollments_path(@course), method: :get, class: "form-inline" do %>
	<%= text_field_tag :search, params[:search], placeholder: "Enter name, email, or SID" %>
	<%= submit_tag "Search", class: "btn btn-primary" %>
<% end %>
