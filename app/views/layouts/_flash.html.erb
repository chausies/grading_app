<strong id="flash">
  <% flash.each do |key, value| %>
		<% values = [*value] # convert to or leave as array %> 
		<% values.each do |value| %>
			<div class="alert alert-<%= key %>"><%= value %></div>
		<% end %>
  <% end %>
</strong>
